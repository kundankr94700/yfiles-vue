import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.netexp.yfiles',
  appName: 'yfilesTesting',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
